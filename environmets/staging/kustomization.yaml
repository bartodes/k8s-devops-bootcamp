apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: prod-api

namePrefix: prod-

commonLabels:
  app: prod-api-store

replicas:
  - name: prod-api-deployment
    count: 1